# MS-005 — PWA shell and local draft storage

**Depends on:** MS-001, MS-002, MS-004

Create stable manifest identity, provisional icon set, prompt-based worker updates, safe offline shell, versioned IndexedDB draft, install guidance, and explicit unavailable-online states.

**Acceptance:** manifest/icon audit passes; first/return/offline/update flows are exercised; guest draft survives reload; registered mutations are never silently queued; browser use remains complete without installation.
